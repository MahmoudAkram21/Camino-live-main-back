# إعداد قاعدة البيانات باستخدام phpMyAdmin 🚀

## الخطوات بالتفصيل

### **الخطوة 1: افتح phpMyAdmin**

1. افتح المتصفح (Chrome, Firefox, etc.)
2. اذهب إلى: `http://localhost/phpmyadmin`
   - أو `http://localhost:8080/phpmyadmin` (حسب إعدادات XAMPP/WAMP)
   - أو الرابط اللي استخدمته عادة

3. **سجل الدخول:**
   - Username: `root`
   - Password: كلمة مرور MySQL (غالباً فاضية أو `root`)
   - اضغط Go

---

### **الخطوة 2: إنشاء قاعدة البيانات**

1. في الشريط العلوي، اضغط على **"New"** أو **"جديد"**
   - أو من القائمة اليسرى، اضغط على **"New"**

2. في صفحة إنشاء قاعدة البيانات:
   - **Database name**: `camino_db`
   - **Collation**: اختر `utf8mb4_unicode_ci`
   - اضغط **"Create"** أو **"إنشاء"**

3. **تأكد**: في القائمة اليسرى، يجب أن ترى `camino_db` ✅

---

### **الخطوة 3: تشغيل ملف schema.sql**

1. **اختر قاعدة البيانات:**
   - من القائمة اليسرى، اضغط على `camino_db`
   - أو من القائمة المنسدلة فوق، اختر `camino_db`

2. **افتح ملف schema.sql:**
   - في الشريط العلوي، اضغط على تبويب **"Import"** أو **"استيراد"**

3. **ارفع الملف:**
   - اضغط على **"Choose File"** أو **"اختر ملف"**
   - اذهب إلى: `C:\Users\Mohammed Alammar\Desktop\Camino V2\backend\database\schema.sql`
   - اختر الملف `schema.sql`

4. **شغّل الاستيراد:**
   - تأكد أن **"Format"** = `SQL`
   - اضغط **"Go"** أو **"إذهب"** في الأسفل

5. **انتظر:** سترى رسالة نجاح في الأخضر ✅

---

### **الخطوة 4: التحقق من النجاح**

1. من القائمة اليسرى، اضغط على `camino_db`
2. اضغط على تبويب **"Structure"** أو **"الهيكل"**
3. **يجب أن ترى 14 جدول:**
   - ✅ users
   - ✅ countries
   - ✅ destinations
   - ✅ collections
   - ✅ trips
   - ✅ trip_images
   - ✅ trip_stops
   - ✅ journal_articles
   - ✅ faqs
   - ✅ reviews
   - ✅ booking_drafts
   - ✅ newsletter_subscriptions
   - ✅ contact_leads
   - ✅ site_settings

**إذا رأيت كل الجداول = نجحت! 🎉**

---

### **الخطوة 5: التحقق من محتوى الجداول (اختياري)**

- اضغط على أي جدول (مثلاً `users`)
- اضغط على تبويب **"Browse"** أو **"تصفح"**
- يجب أن تكون الجداول فاضية (لحد ما تشغّل Seeders)

---

## ✅ بعد ما تخلص

### **1. أنشئ ملف `.env` في مجلد `backend/`**

افتح ملف جديد اسمه `.env` ونسخ فيه:

```env
NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_NAME=camino_db
DB_USER=root
DB_PASSWORD=كلمة_مرور_MySQL_هنا

JWT_SECRET=your_super_secret_jwt_key_change_this
JWT_REFRESH_SECRET=your_refresh_secret_change_this
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

CORS_ORIGIN=http://localhost:5173,http://localhost:5174
```

**ملاحظة مهمة**: 
- إذا كلمة مرور MySQL فاضية، اترك `DB_PASSWORD=` فاضي
- إذا كلمة المرور `root`، اكتب `DB_PASSWORD=root`

---

### **2. ثبّت الحزم**

افتح Terminal في مجلد `backend`:

```bash
cd backend
npm install
```

---

### **3. شغّل Seeders (البيانات الوهمية)**

```bash
npm run seed
```

هذا سيملأ قاعدة البيانات ببيانات وهمية كاملة!

---

### **4. شغّل السيرفر**

```bash
npm run dev
```

السيرفر سيعمل على: `http://localhost:3000` 🚀

---

## 🎯 ملخص سريع

1. ✅ افتح phpMyAdmin
2. ✅ أنشئ قاعدة بيانات `camino_db`
3. ✅ Import ملف `schema.sql`
4. ✅ تحقق من وجود 14 جدول
5. ✅ أنشئ ملف `.env`
6. ✅ `npm install`
7. ✅ `npm run seed`
8. ✅ `npm run dev`

**كده خلصت! 🎉**

---

## ❓ حل المشاكل الشائعة

### **مشكلة: "Access denied" في phpMyAdmin**
- تأكد من كلمة مرور MySQL صحيحة
- جرب `root` أو اتركها فاضية

### **مشكلة: "Cannot import file"**
- تأكد أن الملف `schema.sql` موجود
- المسار: `backend\database\schema.sql`
- تأكد أن حجم الملف أقل من الحد المسموح في phpMyAdmin

### **مشكلة: "Database already exists"**
- مش مشكلة! يعني قاعدة البيانات موجودة
- كمل الخطوات التالية

### **مشكلة: "No tables found"**
- تأكد أنك شغّلت Import بنجاح
- تأكد أنك اخترت قاعدة البيانات `camino_db` قبل Import
- جرب Import مرة تانية

---

## 📸 صور توضيحية (إرشادات)

### **1. إنشاء قاعدة البيانات:**
```
phpMyAdmin > New (في القائمة اليسرى)
> Database name: camino_db
> Collation: utf8mb4_unicode_ci
> Create
```

### **2. Import ملف SQL:**
```
phpMyAdmin > اختر camino_db (من القائمة اليسرى)
> تبويب Import (أعلى الصفحة)
> Choose File > اختر schema.sql
> Go
```

### **3. التحقق:**
```
phpMyAdmin > camino_db > Structure
> يجب أن ترى 14 جدول في القائمة
```

---

**بالتوفيق! إذا عندك أي مشكلة، قولي! 🚀**

