# دليل إعداد قاعدة البيانات - مبسط جداً 🚀

## الخلاصة: إيه اللي لازم أعمله؟

### **نعم، لازم تعمل الخطوة دي!** ✅

**اللي هي:**
```
mysql -u root -p camino_db < database/schema.sql
```

**ليه مهمة؟**
- علشان تنشئ الجداول في قاعدة البيانات (14 جدول)
- بدونها، المشروع مش هيشتغل!

---

## 🎯 الطريقة الأسهل (موصى بها)

### **استخدم MySQL Workbench** (الأسهل على Windows)

1. **افتح MySQL Workbench**
   - افتح البرنامج من Start Menu

2. **اتصل بالسيرفر**
   - اضغط على "Local instance MySQL"
   - ادخل كلمة مرور root

3. **أنشئ قاعدة البيانات**
   - اضغط على أيقونة "Create a new schema" (أو `Ctrl+Shift+N`)
   - اسم: `camino_db`
   - Collation: `utf8mb4_unicode_ci`
   - اضغط Apply

4. **شغّل schema.sql**
   - افتح ملف `backend/database/schema.sql` من File > Open SQL Script
   - **مهم**: من القائمة المنسدلة فوق، اختر قاعدة البيانات `camino_db`
   - اضغط Execute (⚡) أو `Ctrl+Shift+Enter`
   - انتظر لحد ما يخلص

5. **تأكد**
   - في قائمة اليسار، اضغط على `camino_db` > Tables
   - يجب أن ترى 14 جدول ✅

**كده خلصت! 🎉**

---

## 🔧 الطريقة البديلة (Command Line)

### **الطريقة 1: من Command Prompt**

1. **افتح Command Prompt** (cmd)
   - اضغط `Windows + R`
   - اكتب `cmd` واضغط Enter

2. **اذهب لمجلد المشروع:**
```bash
cd "C:\Users\Mohammed Alammar\Desktop\Camino V2\backend"
```

3. **شغّل الأمر:**
```bash
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS camino_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
```
(هيديك كلمة مرور - اكتبها واضغط Enter)

4. **شغّل schema.sql:**
```bash
type database\schema.sql | mysql -u root -p camino_db
```
(هيديك كلمة مرور تاني - اكتبها واضغط Enter)

**كده خلصت! 🎉**

---

### **الطريقة 2: من داخل MySQL**

1. **افتح MySQL Command Line:**
```bash
mysql -u root -p
```

2. **أنشئ قاعدة البيانات:**
```sql
CREATE DATABASE camino_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE camino_db;
```

3. **شغّل schema.sql:**
```sql
SOURCE C:/Users/Mohammed Alammar/Desktop/Camino V2/backend/database/schema.sql;
```
**ملاحظة مهمة**: استخدم `/` بدلاً من `\` في `SOURCE`

4. **تأكد:**
```sql
SHOW TABLES;
```
(يجب أن ترى 14 جدول)

```sql
EXIT;
```

**كده خلصت! 🎉**

---

## ✅ بعد ما تخلص

1. **أنشئ ملف `.env`** في مجلد `backend/`:
```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=camino_db
DB_USER=root
DB_PASSWORD=كلمة_مرور_MySQL_هنا
```

2. **ثبّت الحزم:**
```bash
cd backend
npm install
```

3. **شغّل Seeders (البيانات الوهمية):**
```bash
npm run seed
```

4. **شغّل السيرفر:**
```bash
npm run dev
```

---

## ❓ لو واجهت مشكلة

### **مشكلة: "mysql command not found"**
- استخدم MySQL Workbench (الطريقة الأولى) ✅
- أو أضف MySQL إلى PATH

### **مشكلة: "Access denied"**
- تأكد من كلمة مرور root صحيحة
- تأكد أن MySQL شغال

### **مشكلة: "Database already exists"**
- مش مشكلة! يعني قاعدة البيانات موجودة بالفعل
- كمل الخطوات التالية

---

## 📝 الخلاصة

**نعم، الخطوة دي ضرورية جداً!**

**الأسهل: استخدم MySQL Workbench** ✅
- أسرع
- أسهل
- موضح بالصور

**أو: استخدم Command Line** (إذا كنت مرتاح معه)

**بعدها: كمل باقي الخطوات في `QUICK_START.md`**

---

**بالتوفيق! 🚀**

